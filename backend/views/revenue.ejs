<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .sidebar {
            transition: all 0.3s ease;
        }

        .sidebar:hover {
            background-color: #2d3748;
        }

        .highlight {
            font-weight: bold;
            color: #4ade80;
            /* Màu xanh lá nổi bật */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        .trend {
            font-size: 0.875rem;
            margin-top: 5px;
        }

        .trend-up {
            color: #10b981;
            /* Xanh lá cho tăng */
        }

        .trend-down {
            color: #dc2626;
            /* Đỏ cho giảm */
        }

        .trend-neutral {
            color: #a0aec0;
            /* Xám cho không đổi */
        }
    </style>
</head>

<body class="bg-gray-900 text-gray-100">
    <!-- Sidebar -->
    <div class="flex min-h-screen">
        <div class="sidebar bg-gray-800 w-64 p-6 flex flex-col justify-between">
            <div>
                <h2 class="text-2xl font-bold text-white mb-8">Admin Panel</h2>
                <nav>
                    <a href="/admin"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-tachometer-alt mr-3"></i> Dashboard
                    </a>
                    <a href="/admin/users"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-users mr-3"></i> Quản lý Người dùng
                    </a>
                    <a href="/admin/contacts"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-address-book mr-3"></i> Quản lý Liên hệ
                    </a>
                    <a href="/admin/content-manager"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-file-alt mr-3"></i> Quản lý Content
                    </a>
                    <a href="/admin/history"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-history mr-3"></i> Lịch sử hoạt động của người dùng
                    </a>
                    <a href="/admin/pending-content"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-check-circle mr-3"></i> Xét duyệt nội dung
                    </a>
                    <a href="/admin/ratings"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg">
                        <i class="fas fa-star mr-3"></i> Khảo sát độ hài lòng website
                    </a>

                    <a href="/admin/revenue"
                        class="flex items-center py-3 px-4 text-white bg-indigo-600 rounded-lg mb-2">
                        <i class="fas fa-money-bill-wave mr-3"></i> Quản lý Doanh thu
                    </a>
                    <a href="/admin/revenue/monthly"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-calendar-alt mr-3"></i> Theo Tháng
                    </a>
                    <a href="/admin/revenue/yearly"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg">
                        <i class="fas fa-calendar mr-3"></i> Theo Năm
                    </a>
                    <a href="/admin/user-rank"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-trophy mr-3"></i> Thứ bậc Người dùng
                    </a>
                    <a href="/admin/payment-management"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-credit-card mr-3"></i> Quản lý Thanh toán
                    </a>
                    <a href="/admin/refund"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-undo mr-3"></i> Quản lý Hoàn tiền
                    </a>
                </nav>
            </div>
            <div>
                <form action="/logout" method="GET" style="display: inline;">
                    <button type="submit"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg w-full">
                        <i class="fas fa-sign-out-alt mr-3"></i> Đăng xuất
                    </button>
                </form>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col">
            <!-- Header -->
            <header class="bg-gray-800 p-4 shadow-lg flex justify-between items-center">
                <h1 class="text-xl font-semibold text-white">Quản lý Doanh thu</h1>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-300">Xin chào, Admin</span>
                    <i class="fas fa-user-circle text-2xl text-gray-300"></i>
                </div>
            </header>

            <!-- Content -->
            <div class="p-6">
                <div class="bg-gray-800 rounded-lg shadow-lg p-6">
                    <h2 class="text-2xl font-bold text-white mb-4">Tổng quan Doanh thu</h2>
                    <p class="text-gray-300 mb-6">Dựa trên các giao dịch đã hoàn tất</p>

                    <!-- Thanh lọc khách hàng và tổng doanh thu -->
                    <div class="mb-6 flex justify-between items-center">
                        <div>
                            <label for="userFilter" class="text-white mr-2">Lọc theo khách hàng:</label>
                            <select id="userFilter" name="user_id"
                                onchange="window.location.href='/admin/revenue?user_id=' + this.value"
                                class="p-2 rounded-lg bg-gray-700 text-white">
                                <option value="" <%=!selectedUserId ? 'selected' : '' %>>Tất cả khách hàng</option>
                                <% users.forEach(user=> { %>
                                    <option value="<%= user.id %>" <%=selectedUserId===user.id.toString() ? 'selected'
                                        : '' %>>
                                        <%= user.email %>
                                    </option>
                                    <% }); %>
                            </select>
                            <% let adjustedTotalAmount=0; if (transactions && transactions.length> 0) {
                                adjustedTotalAmount = transactions
                                .filter(t => t.status === 'completed' && (!t.refund_status || t.refund_status !==
                                'PROCESSED'))
                                .reduce((sum, t) => sum + t.amount, 0);
                                }
                                %>
                                <p class="text-white mt-2">Tổng doanh thu: <span class="highlight">
                                        <%= adjustedTotalAmount.toLocaleString('vi-VN') %> VND
                                    </span></p>
                        </div>
                        <!-- Nút xuất Excel ở góc phải -->
                        <a href="/admin/revenue/export"
                            class="bg-green-600 text-white font-bold py-2 px-3 rounded-lg shadow-md hover:bg-green-700 transition-all flex items-center text-sm">
                            <i class="fas fa-download mr-1"></i> Xuất Excel
                        </a>
                    </div>

                    <!-- Doanh thu tháng hiện tại và năm hiện tại -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                        <div class="bg-gray-700 p-4 rounded-lg shadow-md">
                            <h3 class="text-lg font-semibold text-white">Doanh thu Tháng <%= currentMonth %>/<%=
                                        currentYear %>
                            </h3>
                            <% let adjustedMonthlyRevenue=0; if (transactions && transactions.length> 0) {
                                adjustedMonthlyRevenue = transactions
                                .filter(t => t.status === 'completed' && (!t.refund_status || t.refund_status !==
                                'PROCESSED') && new Date(t.created_at).getMonth() === currentMonth - 1 && new
                                Date(t.created_at).getFullYear() === currentYear)
                                .reduce((sum, t) => sum + t.amount, 0);
                                }
                                %>
                                <p class="text-3xl font-bold"><span class="highlight">
                                        <%= adjustedMonthlyRevenue.toLocaleString('vi-VN') %> VND
                                    </span></p>
                                <div
                                    class="trend <%= (monthlyRevenueChangePercent > 0) ? 'trend-up' : (monthlyRevenueChangePercent < 0) ? 'trend-down' : 'trend-neutral' %>">
                                    <% if (prevMonthlyRevenue> 0) { %>
                                        <%= (monthlyRevenueChangePercent> 0) ? '+' : '' %><%=
                                                monthlyRevenueChangePercent %>
                                                %
                                                <%= (monthlyRevenueChangePercent> 0) ? '↑' :
                                                    (monthlyRevenueChangePercent < 0) ? '↓' : '→' %> so với tháng trước
                                                        <% } else { %>
                                                            Không có dữ liệu tháng trước
                                                            <% } %>
                                </div>
                        </div>
                        <div class="bg-gray-700 p-4 rounded-lg shadow-md">
                            <h3 class="text-lg font-semibold text-white">Doanh thu Năm <%= currentYear %>
                            </h3>
                            <% let adjustedYearlyRevenue=0; if (transactions && transactions.length> 0) {
                                adjustedYearlyRevenue = transactions
                                .filter(t => t.status === 'completed' && (!t.refund_status || t.refund_status !==
                                'PROCESSED') && new Date(t.created_at).getFullYear() === currentYear)
                                .reduce((sum, t) => sum + t.amount, 0);
                                }
                                %>
                                <p class="text-3xl font-bold"><span class="highlight">
                                        <%= adjustedYearlyRevenue.toLocaleString('vi-VN') %> VND
                                    </span></p>
                        </div>
                    </div>

                    <!-- Bảng lịch sử giao dịch -->
                    <div class="overflow-x-auto">
                        <table class="w-full text-left border-collapse">
                            <thead>
                                <tr class="bg-gray-700">
                                    <th class="p-3 border-b border-gray-600">ID Giao dịch</th>
                                    <th class="p-3 border-b border-gray-600">Email Khách hàng</th>
                                    <th class="p-3 border-b border-gray-600">Số tiền</th>
                                    <th class="p-3 border-b border-gray-600">Trạng thái</th>
                                    <th class="p-3 border-b border-gray-600">Thời gian</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% transactions.forEach(transaction=> { %>
                                    <tr class="bg-gray-800 hover:bg-gray-700 transition-colors">
                                        <td class="p-3 border-b border-gray-600">
                                            <%= transaction.id %>
                                        </td>
                                        <td class="p-3 border-b border-gray-600">
                                            <%= transaction.user_email %>
                                        </td>
                                        <td class="p-3 border-b border-gray-600">
                                            <%= transaction.amount.toLocaleString('vi-VN') %> VND
                                        </td>
                                        <td class="p-3 border-b border-gray-600">
                                            <%= transaction.status %>
                                        </td>
                                        <td class="p-3 border-b border-gray-600">
                                            <%= new Date(transaction.created_at).toLocaleString("vi-VN") %>
                                        </td>
                                    </tr>
                                    <% }); %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>