<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'Lịch sử' %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .sidebar {
            transition: all 0.3s ease;
        }

        .sidebar:hover {
            background-color: #2d3748;
        }

        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 1.5rem;
        }

        .pagination a {
            color: #a0aec0;
            padding: 0.5rem 1rem;
            margin: 0 0.25rem;
            border-radius: 0.375rem;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .pagination a:hover {
            background-color: #4a5568;
            color: #ffffff;
        }

        .pagination .active {
            background-color: #4a5568;
            color: #ffffff;
            font-weight: bold;
        }

        .pagination .disabled {
            color: #718096;
            pointer-events: none;
        }
    </style>
</head>

<body class="bg-gray-900 text-gray-100">
    <!-- Sidebar -->
    <div class="flex min-h-screen">
        <div class="sidebar bg-gray-800 w-64 p-6 flex flex-col justify-between">
            <div>
                <h2 class="text-2xl font-bold text-white mb-8">Admin Panel</h2>
                <nav>
                    <a href="/admin"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-tachometer-alt mr-3"></i> Dashboard
                    </a>
                    <a href="/admin/users"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-users mr-3"></i> Quản lý Người dùng
                    </a>
                    <a href="/admin/contacts"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-address-book mr-3"></i> Quản lý Liên hệ
                    </a>
                    <a href="/admin/content-manager"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-file-alt mr-3"></i> Quản lý Content
                    </a>
                    <a href="/admin/history"
                        class="flex items-center py-3 px-4 text-white bg-indigo-600 rounded-lg mb-2">
                        <i class="fas fa-history mr-3"></i> Lịch sử hoạt động người dùng
                    </a>
                    <a href="/admin/pending-content"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg">
                        <i class="fas fa-check-circle mr-3"></i> Xét duyệt nội dung
                    </a>
                    <a href="/admin/ratings"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg">
                        <i class="fas fa-star mr-3"></i> Khảo sát độ hài lòng website
                    </a>
                    <a href="/admin/revenue"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg">
                        <i class="fas fa-money-bill-wave mr-3"></i> Quản lý Doanh thu
                    </a>
                    <a href="/admin/user-rank"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-trophy mr-3"></i> Thứ bậc Người dùng
                    </a>
                </nav>
            </div>
            <div>
                <a href="/"
                    class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg">
                    <i class="fas fa-sign-out-alt mr-3"></i> Đăng xuất
                </a>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col">
            <!-- Header -->
            <header class="bg-gray-800 p-4 shadow-lg flex justify-between items-center">
                <h1 class="text-xl font-semibold text-white">
                    <%= typeof title !=='undefined' ? title : 'Lịch sử' %>
                </h1>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-300">Xin chào, Admin</span>
                    <i class="fas fa-user-circle text-2xl text-gray-300"></i>
                </div>
            </header>

            <!-- Content -->
            <div class="p-6">
                <div class="bg-gray-800 rounded-lg shadow-lg p-6">
                    <h2 class="text-2xl font-bold text-white mb-4">Lịch sử hoạt động</h2>
                    <p class="text-gray-300 mb-6">Xem và quản lý lịch sử các hoạt động đã thực hiện.</p>

                    <!-- Bảng hiển thị lịch sử -->
                    <div class="overflow-x-auto">
                        <table class="min-w-full bg-gray-700 rounded-lg">
                            <thead>
                                <tr>
                                    <th class="py-3 px-4 text-left text-gray-300">ID</th>
                                    <th class="py-3 px-4 text-left text-gray-300">Người dùng</th>
                                    <th class="py-3 px-4 text-left text-gray-300">Chủ đề</th>
                                    <th class="py-3 px-4 text-left text-gray-300">Loại</th>
                                    <th class="py-3 px-4 text-left text-gray-300">Ngôn ngữ</th>
                                    <th class="py-3 px-4 text-left text-gray-300">Nội dung</th>
                                    <th class="py-3 px-4 text-left text-gray-300">Ngày tạo</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (history && history.length> 0) { %>
                                    <% history.forEach(record=> { %>
                                        <tr class="border-b border-gray-600">
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= record.id %>
                                            </td>
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= record.user_email %>
                                            </td>
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= record.topic %>
                                            </td>
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= record.category %>
                                            </td>
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= record.language %>
                                            </td>
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= record.content.length> 50 ? record.content.substring(0, 50) + '...'
                                                    : record.content %>
                                            </td>
                                            <td class="py-3 px-4 text-gray-100">
                                                <%= new Date(record.created_at).toLocaleString('vi-VN') %>
                                            </td>
                                        </tr>
                                        <% }) %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="7" class="py-3 px-4 text-center text-gray-400">Không có
                                                        dữ liệu lịch sử.</td>
                                                </tr>
                                                <% } %>
                            </tbody>
                        </table>
                    </div>

                    <!-- Phân trang -->
                    <% if (history && history.length> 0) { %>
                        <div class="pagination">
                            <!-- Nút Previous -->
                            <a href="/admin/history?page=<%= currentPage - 1 %>"
                                class="<%= currentPage === 1 ? 'disabled' : '' %>">
                                <i class="fas fa-chevron-left"></i>
                            </a>

                            <!-- Các số trang -->
                            <% for (let i=1; i <=totalPages; i++) { %>
                                <a href="/admin/history?page=<%= i %>" class="<%= currentPage === i ? 'active' : '' %>">
                                    <%= i %>
                                </a>
                                <% } %>

                                    <!-- Nút Next -->
                                    <a href="/admin/history?page=<%= currentPage + 1 %>"
                                        class="<%= currentPage === totalPages ? 'disabled' : '' %>">
                                        <i class="fas fa-chevron-right"></i>
                                    </a>
                        </div>
                        <% } %>
                </div>
            </div>
        </div>
    </div>
</body>

</html>