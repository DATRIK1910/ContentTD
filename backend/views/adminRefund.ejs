<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'Quản lý Hoàn tiền' %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .sidebar {
            transition: all 0.3s ease;
        }

        .sidebar:hover {
            background-color: #2d3748;
        }

        .table-container {
            max-height: 600px;
            overflow-y: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #4a5568;
        }

        th {
            background-color: #4a5568;
            color: white;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        tr:hover {
            background-color: #2d3748;
        }

        .confirm-btn {
            transition: all 0.3s ease;
            background-color: #10b981;
            color: white;
            padding: 5px 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-left: 5px;
        }

        .confirm-btn:hover {
            background-color: #059669;
        }

        .loading {
            opacity: 0.6;
            pointer-events: none;
        }
    </style>
</head>

<body class="bg-gray-900 text-gray-100">
    <!-- Sidebar -->
    <div class="flex min-h-screen">
        <div class="sidebar bg-gray-800 w-64 p-6 flex flex-col justify-between">
            <div>
                <h2 class="text-2xl font-bold text-white mb-8">Admin Panel</h2>
                <nav>
                    <a href="/admin"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-tachometer-alt mr-3"></i> Dashboard
                    </a>
                    <a href="/admin/users"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-users mr-3"></i> Quản lý Người dùng
                    </a>
                    <a href="/admin/contacts"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-address-book mr-3"></i> Quản lý Liên hệ
                    </a>
                    <a href="/admin/content-manager"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-file-alt mr-3"></i> Quản lý Content
                    </a>
                    <a href="/admin/history"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-history mr-3"></i> Lịch sử hoạt động của người dùng
                    </a>
                    <a href="/admin/pending-content"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-check-circle mr-3"></i> Xét duyệt nội dung
                    </a>
                    <a href="/admin/ratings"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-star mr-3"></i> Khảo sát độ hài lòng website
                    </a>
                    <a href="/admin/revenue"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-money-bill-wave mr-3"></i> Quản lý Doanh thu
                    </a>
                    <a href="/admin/user-rank"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-trophy mr-3"></i> Thứ bậc Người dùng
                    </a>
                    <a href="/admin/payment-management"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg mb-2">
                        <i class="fas fa-credit-card mr-3"></i> Quản lý Thanh toán
                    </a>
                    <a href="/admin/refund"
                        class="flex items-center py-3 px-4 text-white bg-indigo-600 rounded-lg mb-2">
                        <i class="fas fa-undo mr-3"></i> Quản lý Hoàn tiền
                    </a>
                </nav>
            </div>
            <div>
                <form action="/logout" method="GET" style="display: inline;">
                    <button type="submit"
                        class="flex items-center py-3 px-4 text-gray-300 hover:bg-gray-700 hover:text-white rounded-lg w-full">
                        <i class="fas fa-sign-out-alt mr-3"></i> Đăng xuất
                    </button>
                </form>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col">
            <!-- Header -->
            <header class="bg-gray-800 p-4 shadow-lg flex justify-between items-center">
                <h1 class="text-xl font-semibold text-white">
                    <%= typeof title !=='undefined' ? title : 'Quản lý Hoàn tiền' %>
                </h1>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-300">Xin chào, Admin</span>
                    <i class="fas fa-user-circle text-2xl text-gray-300"></i>
                </div>
            </header>

            <!-- Content -->
            <div class="p-6">
                <div class="bg-gray-800 rounded-lg shadow-lg p-6 mb-6">
                    <h2 class="text-2xl font-bold text-white mb-4">Quản lý Hoàn tiền</h2>
                    <div class="table-container">
                        <table id="refundsTable">
                            <thead>
                                <tr>
                                    <th>Mã Giao Dịch</th>
                                    <th>Người Dùng</th>
                                    <th>Số Tiền (VND)</th>
                                    <th>Lý Do</th>
                                    <th>Trạng Thái</th>
                                    <th>Thời Gian</th>
                                    <th>Hành Động</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% refunds.forEach(refund=> { %>
                                    <tr>
                                        <td>
                                            <%= refund.transaction_id %>
                                        </td>
                                        <td>
                                            <%= refund.user_id %>
                                        </td>
                                        <td>
                                            <%= refund.refund_amount.toLocaleString() %>
                                        </td>
                                        <td>
                                            <%= refund.reason %>
                                        </td>
                                        <td>
                                            <span
                                                class="px-2 py-1 text-xs <%= refund.status === 'PENDING' ? 'bg-yellow-100 text-yellow-800' : refund.status === 'VERIFIED' ? 'bg-blue-100 text-blue-800' : 'bg-green-100 text-green-800' %>">
                                                <%= refund.status %>
                                            </span>
                                        </td>
                                        <td>
                                            <%= new Date(refund.request_date).toLocaleString('vi-VN') %>
                                        </td>
                                        <% if (refund.status==='PENDING' || refund.status==='VERIFIED' ) { %>
                                            <td>
                                                <form id="confirmForm_<%= refund.id %>" action="/admin/process-refund"
                                                    method="POST" style="display: inline;"
                                                    onsubmit="return confirmRefund('<%= refund.id %>', event);">
                                                    <input type="hidden" name="refund_id" value="<%= refund.id %>">
                                                    <button type="submit" class="confirm-btn"
                                                        data-refund-id="<%= refund.id %>">
                                                        <i class="fas fa-check mr-1"></i> Xác nhận
                                                    </button>
                                                </form>
                                            </td>
                                            <% } else if (refund.status==='PROCESSED' ) { %>
                                                <td>Hoàn tiền hoàn tất</td>
                                                <% } else { %>
                                                    <td>Đã từ chối</td>
                                                    <% } %>
                                    </tr>
                                    <% }); %>
                            </tbody>
                        </table>
                    </div>
                    <% if (refunds.length===0) { %>
                        <p class="text-gray-300 mt-4">Không có yêu cầu hoàn tiền nào.</p>
                        <% } %>
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        function confirmRefund(refundId, event) {
            event.preventDefault();

            const $form = $(`#confirmForm_${refundId}`);
            const $row = $form.closest('tr');
            $row.addClass('loading');

            $.ajax({
                url: '/admin/process-refund',
                type: 'POST',
                data: $form.serialize(),
                success: function (response) {
                    if (response.success) {
                        $row.find('td:nth-child(5) span').removeClass('bg-yellow-100 text-yellow-800 bg-blue-100 text-blue-800')
                            .addClass('bg-green-100 text-green-800')
                            .text('PROCESSED');
                        $row.find('td:nth-child(7)').text('Hoàn tiền hoàn tất');
                        alert('Xác nhận hoàn tiền thành công!');
                    } else {
                        alert('Xác nhận thất bại: ' + response.message);
                    }
                },
                error: function (xhr, status, error) {
                    alert('Lỗi: ' + error);
                },
                complete: function () {
                    $row.removeClass('loading');
                }
            });
        }
    </script>
</body>

</html>